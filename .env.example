# Runtime selection: fake or pi
MODELING_AGENT_RUNTIME=fake

# Optional pi model override. Otherwise pi uses its configured default.
# MODELING_AGENT_PROVIDER=anthropic
# MODELING_AGENT_MODEL=claude-sonnet-4-5
# MODELING_AGENT_THINKING=high

MODELING_AGENT_HOST=127.0.0.1
MODELING_AGENT_PORT=4317
